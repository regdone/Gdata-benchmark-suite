# === Core ML Frameworks ===
# TensorFlow (GPU-enabled build) – supports CUDA 12.x, cuDNN 9+
tensorflow>=2.18

# PyTorch + CUDA (chọn bản khớp driver/cuda toolkit trên máy khách)
# Nếu pip install mặc định fail -> khuyến nghị khách theo hướng dẫn https://pytorch.org/get-started/locally/
torch>=2.3
torchvision>=0.18
torchaudio>=2.3

# === Hugging Face (NLP / Transformers) ===
transformers>=4.43
accelerate>=0.33

# === Utilities ===
psutil>=5.9
